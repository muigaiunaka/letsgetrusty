fn main() {
    // prints the line hello world
    println!("Hello, World!");
    // then A binary can be generated using the Rust compiler: rustc.
    // `rustc 00-hello-world`
    // this should create a file that matches the name of the file
}